X ← {8, 4, 6, 10, 3, 7, 9, 5};
Y ← {77, 43, 55, 67, 65, 41, 82, 69};
N ← |X|;
Xᵢ: (i) → ᵢ(X, i);
Yᵢ: (i) → ᵢ(Y, i);
Σ: (s, e, f) →
  if s < e: f(s) + Σ(s + 1, e, f)
  otherwise: f(e);

Aᵪ ← 1/N · Σ(1, N, Xᵢ);
Aᵧ ← 1/N · Σ(1, N, Yᵢ);

Σσᵪ: (i) → (Xᵢ(i) - Aᵪ) ^ 2;
σᵪ ← √( 1/(N - 1) · Σ(1, N, Σσᵪ) );

Σσᵧ: (i) → (Yᵢ(i) - Aᵧ) ^ 2;
σᵧ ← √( 1/(N - 1) · Σ(1, N, Σσᵧ) );

Σcov: (i) → (Xᵢ(i) - Aᵪ) · (Yᵢ(i) - Aᵧ);
covᵪᵧ ← Σ(1, N, Σcov)/(N - 1);

ρᵪᵧ ← covᵪᵧ / (σᵪ · σᵧ);
≈(ρᵪᵧ, 5);